# Elite Tour USA — новый сайт

Статический сайт частного гида по США (русскоязычная аудитория). Без бэкенда,
без сборщиков и зависимостей: чистые HTML/CSS/JS, генерируются одним node-скриптом.

```
data/site.json     ← весь текст, цены, программы, FAQ. Правится здесь.
build.mjs          ← генерирует страницы из site.json
site/              ← готовый сайт, его и заливать на хостинг
  index.html
  tury/<slug>/index.html   × 16
  assets/style.css, assets/app.js, assets/img/*.jpg
  sitemap.xml, robots.txt
```

## Пересобрать

```bash
node build.mjs
```

Скрипт печатает предупреждения, если что-то требует внимания (см. «Что доделать»).

## Посмотреть локально

```bash
python3 -m http.server 8412 --directory site
```

Открыть http://localhost:8412

## Что здесь сделано и почему

Решения приняты после разбора старого сайта и совета четырёх моделей
(GPT-5.6 Sol, Gemini 3.1 Pro, Grok 4.5, Kimi K3). Основное:

- **Цены на витрине.** На старом сайте они были спрятаны внутри страниц; восемь
  экскурсий имеют конкретную стоимость — теперь она в карточках, в отдельном
  прайс-листе и в микроразметке. Это главное отличие от прототипа ChatGPT,
  где цен не было вообще.
- **Не «люкс», а конкретика.** Тёмно-синий с золотом и Playfair — дефолтный
  ИИ-шаблон «luxury travel»; для гида с ассортиментом от Диснейленда до
  стрельбища он читается как обёртка без ответственности. Взят документально-
  редакционный язык: выгоревшая бумага, асфальт, терракота, Unbounded + Onest +
  JetBrains Mono, данные (цена / часы / что входит) как основа композиции.
- **WhatsApp-first.** Каждая карточка и каждая страница ведёт в WhatsApp с уже
  подставленным текстом. Форма ничего не отправляет на сервер — она собирает
  сообщение и открывает чат, поэтому заявки не теряются в спаме.
- **Отдельный URL на каждую экскурсию** (`/tury/<slug>/`) с микроразметкой
  `TouristTrip`, плюс `FAQPage` и `TravelAgency` на главной, sitemap и robots.
- **16 услуг разложены по намерению** на пять категорий, а не свалены в
  выпадающее меню на 16 пунктов.
- **Убрано:** Login / Sign Up (регистрация на сайте гида ничего не давала).
  «Продвижение в соцсетях» вынесено из каталога экскурсий в блок «С чем ещё
  помогаю» — в каталоге туров оно разрушало позиционирование.

## Где сайт живёт

- **Онлайн:** https://damonbogdan.github.io/elitetourusa/
- **Репозиторий:** https://github.com/damonbogdan/elitetourusa
- Ветка `main` — исходники, ветка `gh-pages` — опубликованная папка `site/`.

Выкатить изменения:

```bash
node build.mjs && git add -A && git commit -m "..." && git push origin main && git subtree push --prefix site origin gh-pages
```

Чтобы переехать на `elitetourusa.com`: в настройках репозитория Pages → Custom
domain, а у регистратора — CNAME на `damonbogdan.github.io`.

## Фотографии

Часть кадров — настоящие, взяты со старого сайта:

| Где | Что |
|---|---|
| «О гиде» | Геннадий на 75-й церемонии «Эмми» (превью с его YouTube) |
| Стрельбище | Геннадий на стрельбище |
| Лас-Вегас | Геннадий с группой у знака Las Vegas |
| Голливуд, Гетти, Universal, «Дель Коронадо», храм Hsi Lai, крокодил, Burning Man | фото со старого сайта |

Остальные (главный экран, Санта-Моника, Гранд-Каньон, Диснейленд, SeaWorld,
ночной LA, VIP) — сгенерированы Nano Banana 2: пейзажи и виды, без людей и лиц.
Заменяются в любой момент — положите свой JPEG под тем же именем в
`assets/img/` и пересоберите. Оригиналы ИИ-версий лежат в
`assets/img/ai-backup/` (не коммитятся).

**Сознательно не взял со старого сайта** рекламные композиции Disney и SeaWorld
и стоковое фото пары у частного самолёта: это, скорее всего, чужие материалы,
и переносить их на новый коммерческий сайт рискованно. На их местах — свои
сгенерированные кадры. Если у Геннадия есть собственные фото из этих поездок,
они лучше любого варианта.

## Самодиагностика

Сайт умеет проверять сам себя. Проверяется то, из-за чего он реально может
«поехать»: не применился CSS, не подгрузились шрифты, упал `app.js` (и весь
контент остался невидимым), битые картинки, горизонтальный скролл, пустой
каталог или прайс, сломанные ссылки на WhatsApp, ошибки JavaScript.

**Посмотреть глазами:**

| Адрес | Что делает |
|---|---|
| `любая-страница/?diag=1` | панель с результатами поверх страницы |
| `/diag.html` | стенд: прогоняет сайт в ифреймах на 375 / 414 / 768 / 1024 / 1280 / 1440 и собирает общий отчёт |

**Прогнать из терминала:**

```bash
node check.mjs
```

Проверяет опубликованный сайт (или локальный, если передать адрес:
`node check.mjs http://localhost:8412`). Код выхода: `0` — чисто, `1` —
замечания, `2` — критические проблемы. Подробности — в `diag-report.json`.

**Что следит само:**

- `.github/workflows/diag.yml` — прогон при каждом пуше и ежедневно в 06:17 UTC.
  При поломке заводит issue с меткой `diag` (GitHub пришлёт письмо), при
  починке — закрывает его.
- Ежедневная задача в Claude в 10:05 — приносит результат прямо в переписку.
  Работает, пока приложение открыто; если Mac был выключен, отработает при
  следующем запуске. Сторож в CI от этого не зависит.

**Страховка.** Блоки на сайте появляются с анимацией, то есть до срабатывания
скрипта они прозрачные. Если `app.js` почему-то не загрузится, инлайн-скрипт в
`<head>` через 4 секунды сам снимет класс `js` — контент станет видимым, пусть
и без анимации. Молчаливого белого экрана не будет.

Проверено намеренной поломкой: с убитым CSS, сломанным `app.js` и удалённой
картинкой диагностика находит все три причины и возвращает код 2.

## Что доделать

**1. Четыре ответа в FAQ я написал по здравому смыслу, а не взял со старого
сайта.** Их нужно подтвердить или переписать в `data/site.json`:

- «На чём поедем?» — предполагается личный автомобиль гида;
- «Едем с детьми — это нормально?» — про детские кресла;
- «Как оплатить?» — предполагается расчёт на месте без предоплаты;
- «Что если планы поменяются?» — предполагается перенос без штрафа.

Сборка каждый раз о них напоминает. Пока не подтверждены — это обещания от
имени Геннадия, которых он не давал.

**2. Отзывы.** Перенесены четыре штуки со старого сайта: без фото, дат и
маршрутов они слабые. Сильнее всего сработают скриншоты реальной переписки
(с замазанными номерами) или видеоотзывы с YouTube-канала.

**3. Домен и `domain` в `data/site.json`.** Сейчас `https://elitetourusa.com` —
от этого значения считаются canonical, Open Graph и sitemap. Если адрес другой,
поменяйте и пересоберите.

## Публикация

Папка `site/` — обычная статика, кладётся на любой хостинг как есть:
Netlify, Cloudflare Pages, GitHub Pages, или просто в корень текущего сервера
вместо WordPress. Бэкенд, база и PHP не нужны.
